# Frontend environment (copy to .env.local)
# Leave empty to use the Vite dev proxy (/v1 -> http://127.0.0.1:8080).
# On Windows, large workflow image uploads may fail through the proxy; use direct API:
# VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_API_BASE_URL=
VITE_APP_ENV=development
