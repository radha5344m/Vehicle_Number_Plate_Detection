# Application Services

Optional application-layer services for cross-cutting orchestration.

**Prefer use cases** — only add services when logic does not fit a single use case.

---

## Placeholders

| File | Purpose |
|------|---------|
| `error_mapper.placeholder` | Map domain errors to application results _TBD_ |

---

## Status

Empty at foundation — expand only if use cases prove insufficient.
